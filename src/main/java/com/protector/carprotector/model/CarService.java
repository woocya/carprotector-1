package com.protector.carprotector.model;

/*@Configuration
@ComponentScan
public class CarService {

    @Bean
    public Car getMyCar() {
        Generators draw = new Generators();
        draw.addLocation();

        int whichLocation = 0;
        Location myCarLocation = new Location(draw.getKey(whichLocation), draw.getValue(whichLocation));
        Engine engine = new Engine(false, false, LocalDate.now(), LocalTime.now());

        StateOfCar state = new StateOfCar(myCarLocation, engine);

        Limitations limits = new Limitations(LocalTime.parse("09:00:00"), LocalTime.parse("23:00:00"), new Coordinates(51.65, "N", 19.4, "E"), new Coordinates(51.9, "N", 19.6, "E"), false);

        Car myCar = new Car("Toyota CH-R", 2018, "Hybrid", "EL-345WR", LocalDate.of(2020,01,01), state, limits);

        //myCar.printInfo();
        return myCar;
    }
}*/
